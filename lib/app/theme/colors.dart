import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const Color lightBackground = Color(0xFFFAFAFA);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightText = Color(0xFF1F1F1F);
  static const Color lightTextSecondary = Color(0xFF616161);

  // Dark theme colors
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkText = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFFBDBDBD);

  // Severity colors
  static const Color criticalRed = Color(0xFFD13438);
  static const Color majorYellow = Color(0xFFFFB900);
  static const Color minorBlue = Color(0xFF3B78FF);

  // Status colors
  static const Color statusOnline = Color(0xFF07A41E); // Green
  static const Color statusOffline = Color(0xFFD13438); // Red
  static const Color statusMaintenance = Color(0xFFFFB900); // Yellow

  // Semantic colors
  static const Color success = Color(0xFF07A41E);
  static const Color warning = Color(0xFFFFB900);
  static const Color error = Color(0xFFD13438);
  static const Color info = Color(0xFF3B78FF);

  // Primary brand
  static const Color primary = Color(0xFF0043CE);
  static const Color primaryLight = Color(0xFF5A8BFF);
  static const Color primaryDark = Color(0xFF002FA9);

  // Neutral
  static const Color divider = Color(0xFFE0E0E0);
  static const Color disabled = Color(0xFFBDBDBD);
  static const Color border = Color(0xFFDFDFDF);

  // Overlay
  static const Color overlayLight = Color(0x1F000000);
  static const Color overlayDark = Color(0x42000000);
}
